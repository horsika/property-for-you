export interface SocialRegisterModel {
  email: string;
  firstName: string;
  lastName: string;
  photoUrl: string;
}

export class SocialRegister {
  email: string;
  firstName: string;
  lastName: string;
  photoUrl: string
}
